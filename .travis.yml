language: java

jdk:
  - oraclejdk8

sudo: required

services:
  - docker

install: true

script: mvn clean package docker:build -DskipTests -e

after_success:
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker push mihir83in/currencyconverter
